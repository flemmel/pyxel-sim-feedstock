{% set name = "pyxel-sim" %}
{% set version = "1.6" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/pyxel_sim-{{ version }}.tar.gz
  sha256: 599650f4dab962852be5f3e99a26d4cc9faf6f295bc3c93a7040d523fd13b74f

build:
  number: 0
  noarch: python
  entry_points:
    - pyxel = pyxel.run:main
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.7
    - setuptools >=40.1
    - wheel >=0.29.0
  run:
    - astropy >=4.3
    - attrs >=21.3.0
    - bokeh
    - dask-core
    - distributed
    - fsspec >=2021
    - h5py
    - holoviews >=1.15.0
    - ipywidgets >=7.5
    - matplotlib-base >=3.1
    - numba
    - numpy >=1.20
    - openpyxl
    - pandas
    - pillow
    - pympler
    - pyyaml
    - scikit-image
    - scipy
    - seaborn
    - tqdm
    - typing-extensions
    - xarray >=0.19
    - xlrd
    - pygmo       # Added manually
    - netcdf4

test:
  imports:
    - pyxel
    - pyxel.calibration
  commands:
    - pip check
    - pyxel --help
  requires:
    - pip

about:
  home: https://esa.gitlab.io/pyxel/
  summary: Pyxel detector simulation framework.
  dev_url: https://gitlab.com/esa/pyxel
  license: MIT
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - flemmel
    - constanzeseibert
